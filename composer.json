{
    "require-dev": {
        "phpunit/phpunit": "^12.3",
        "phpstan/phpstan": "^2.1"
    },
    "require": {
        "vlucas/phpdotenv": "^5.6"
    },
    "scripts": {
        "phpstan": "./vendor/bin/phpstan analyze -c phpstan.neon"
    }
}
